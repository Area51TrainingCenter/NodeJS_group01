html
	head
		meta(charset="utf8")
	body
		a(href="/add-form") Agregar película
		-if(data.length)
			table
				tr
					th Id
					th Título
					th Año
				-each item in data
					tr
						td #{item.idpelicula}
						td #{item.titulo}
						td #{item.anno}
		-else
			h1 No existen registros en la base de datos